"""Pytest config for the claude-plugin tree.

Unlike the Hermes plugin (which has a Python `src/guide_plugin/` lib),
the Claude Code plugin is fully declarative — there's no Python code to
import, so this file is intentionally minimal. Reserved for future hooks.
"""
